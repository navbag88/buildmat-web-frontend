# Copy this to .env.local for local development.
# In Railway, set VITE_API_BASE_URL as an environment variable on the frontend service.

# Point to the gateway (preferred) or directly to the backend
VITE_API_BASE_URL=http://139.59.46.39:8090/api

# For local dev without the gateway running:
# VITE_API_BASE_URL=http://localhost:8080/api
